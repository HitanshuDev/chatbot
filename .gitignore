node_modules/
dist/
build/
.next/

.env
.env.local
.env.*.local

*.log
npm-debug.log*
coverage/
.DS_Store
.vscode/
.idea/
*.tsbuildinfo

# pnpm falls back to a project-local store when it cannot hard-link to the
# default one (happens if pnpm install is run inside the container).
.pnpm-store/
